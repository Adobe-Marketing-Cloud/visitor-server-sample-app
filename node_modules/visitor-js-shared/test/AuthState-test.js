const test = require("tape");
const { AuthState } = require("../src");

test("AuthState:", (t) => {
    t.ok(AuthState, "AuthState should be exported correctly.");
    t.equal(AuthState.UNKNOWN, 0);
    t.end();
});
