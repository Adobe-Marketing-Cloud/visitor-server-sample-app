"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
var cookies = {
    // TODO Consider moving this logic into `visitor-js-shared`.
    // TODO What if the values in the cookie have expired?
    parseValuesFromAmcvCookie: function parseValuesFromAmcvCookie() {
        var cookie = arguments.length <= 0 || arguments[0] === undefined ? "" : arguments[0];
        var values = arguments.length <= 1 || arguments[1] === undefined ? [] : arguments[1];

        var tokens = decodeURI(cookie).split("|");

        return values.reduce(function (amcvVals, val) {
            var _iteratorNormalCompletion = true;
            var _didIteratorError = false;
            var _iteratorError = undefined;

            try {
                for (var _iterator = tokens[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
                    var param = _step.value;

                    if (param.indexOf(val) === 0) {
                        var indexOfParam = tokens.indexOf(param);
                        amcvVals[val] = tokens[indexOfParam + 1];
                        return amcvVals;
                    }
                }
            } catch (err) {
                _didIteratorError = true;
                _iteratorError = err;
            } finally {
                try {
                    if (!_iteratorNormalCompletion && _iterator.return) {
                        _iterator.return();
                    }
                } finally {
                    if (_didIteratorError) {
                        throw _iteratorError;
                    }
                }
            }
        }, {});
    }
};

exports.default = cookies;
module.exports = exports["default"];